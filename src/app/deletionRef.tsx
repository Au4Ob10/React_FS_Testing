import React, {createRef} from 'react'

// interface clearMessage {
//  string 
// }

export const clearMessageRef = createRef()
export const deleteLetterRef = createRef()


clearMessageRef.current = null;
deleteLetterRef.current = null;

